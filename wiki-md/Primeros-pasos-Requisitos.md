# Requisitos


## Software necesario

| Herramienta             | Versión mínima | Uso                            |
|-------------------------|----------------|--------------------------------|
| a                       | `0`            | \[frontend / tooling / no se\] |
| *lenguaje backend*      | `x.y`          | servicios / api                |
| Docker + Docker Compose | `24.x`         | entorno local                  |
| Git                     | `2.40+`        | control de versiones           |

## Cuentas y accesos

- Acceso al repositorio.
- Credenciales para servicios externos (si consumimos apis con api keys
  NO PONERLAS solo mencionar que usamos).
- Acceso a un gestor de secretos / `.env` de ejemplo.

## Verificación rápida

``` bash
lenguaje_back --version
docker --version
git --version
```
